{
  "name": "nuxest",
  "private": true,
  "description": "Scaffold NestJS + Nuxt monorepos with batteries included",
  "packageManager": "pnpm@11.10.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm --filter nuxest build && pnpm --filter create-nuxest build",
    "dev": "pnpm --filter nuxest dev",
    "create": "pnpm --filter create-nuxest dev",
    "create:nuxest": "pnpm --filter nuxest dev",
    "dev:stack": "concurrently -n api,web -c blue,green \"pnpm --filter api dev\" \"pnpm --filter web dev\"",
    "test:scaffold": "node packages/nuxest/dist/test-scaffold.js",
    "test": "pnpm build && node packages/nuxest/dist/test-scaffold.js nuxest-smoke-test"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
